Screenshots go here once the app is run locally. Not included in this build since no browser
was available to capture live UI renders in the environment this project was generated in —
run `npm run dev` + `uvicorn main:app --reload` (Demo Mode needs no API key) and drop PNGs here.

Suggested set, matching the homepage top-to-bottom:
1. `market-mood.png` — the Market Mood card (Calm/Watchful/Active/Volatile)
2. `todays-replay.png` — the Morning/Afternoon/Now tabbed replay
3. `portfolio-heatmap.png` — the colored tile grid
4. `attention-feed.png` — Priority Cards vs. Quiet Watchlist, with the Attention Score meter and "Why Quiet?" section visible on at least one card of each
5. `quick-attention.png` — the floating ⚡ button and the glow highlight on its target card
6. `add-stock.png`, `stock-detail.png` — unchanged flows, kept for completeness
